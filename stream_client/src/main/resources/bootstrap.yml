server:
  port: 8663
spring:
  application:
    name: stream-client
  profiles:
    active: dev
  cloud:
    config:
      #通过IP加端口访问
      uri: http://localhost:8769
      #启动失败时能够快速响应
      fail-fast: true
